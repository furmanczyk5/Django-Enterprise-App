---

# Django model admin permissions for the all wagtail chapter and divisions groups. This is merely required so admins
#    have the ability to edit and approve jobs posts

# The staff users who belong in this group.
# Values are usernames (i.e. APA id)
# This is set by the staff_teams field, do not directly assign any here
staff_users:
  -

# The value of :attr:`django.contrib.auth.models.Permission.codename`
# :attr:`django.contrib.auth.models.ContentType.model`
# split on the _ for adding Permissions codenames
app_models_granted:
  - jobs_job
  - jobs_wagtailjob
